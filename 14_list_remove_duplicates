#Write a program (function!) that takes a list and returns a new list that contains all the elements of the first list minus all the duplicates.
def duplicate_remover():
  a = [1,2,3,4,4,4,4,4,5,5,6,7,8,9,10]
  b = list(set(a))
  print(b)
duplicate_remover()
